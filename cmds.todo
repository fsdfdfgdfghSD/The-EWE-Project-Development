MKDIR (MD)

Options:
-v (verbose)
-p (parent)

Examples:
1. md -v k
    md: created directory 'k'

2. md -vp l/d/g
    mkdir: created directory 'l'  
    mkdir: created directory 'l/d'
    mkdir: created directory 'l/d/g'